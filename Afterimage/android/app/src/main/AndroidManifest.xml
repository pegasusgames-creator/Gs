<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.android.vending.BILLING" />

    <!-- Sister Pegasus Games packages for cross-promo "installed?"
         verification — must match CROSS_PROMO_PACKAGES in MainActivity.java
         and PROMO_GAMES in game.html. LIVE Play Store apps only. -->
    <queries>
        <package android:name="com.pegasusgames.watersortpuzzle" />
        <package android:name="com.pegasusgames.nonogram" />
        <package android:name="com.pegasusgames.puzzle2048" />
        <package android:name="com.pegasusgames.unblockpuzzle" />
        <package android:name="com.pegasusgames.pipeconnect" />
        <package android:name="com.pegasusgames.hunch" />
        <package android:name="com.pegasusgames.overlay" />
    </queries>
    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="Afterimage"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PipeConnect"
        android:hardwareAccelerated="true"
        android:pageSizeCompat="enabled">
        <!-- Play Games Services APP_ID — shared Pegasus Games PGS project.
             Add the PipeConnect credential in Play Console -> Play Games Services
             before relying on real leaderboards (synthetic fallback until then). -->
        <meta-data android:name="com.google.android.gms.games.APP_ID"
                   android:value="@string/games_app_id" />


        <!-- Resolve AdMob vs Firebase Analytics conflict over AD_SERVICES_CONFIG -->
        <property
            android:name="android.adservices.AD_SERVICES_CONFIG"
            android:resource="@xml/gma_ad_services_config"
            tools:replace="android:resource" />

        <meta-data android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-2759523698880843~1438975250"/>
        <activity android:name=".MainActivity" android:exported="true"
 android:configChanges="orientation|screenSize|keyboardHidden" android:windowSoftInputMode="adjustNothing">
            <intent-filter><action android:name="android.intent.action.MAIN"/><category android:name="android.intent.category.LAUNCHER"/></intent-filter>
        </activity>
    

        <receiver
            android:name=".NotificationReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
