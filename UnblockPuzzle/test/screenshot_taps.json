{
  "_comment": "GAMEPLAY ONLY (2026-06-18 policy) — 7 phone + 2 tablet_7 + 2 tablet_10 = 11 slots, every one an ACTUAL gameplay board at a DISTINCT level. No menu/shop/settings/themes/stats/level-select/ranks/missions/daily page and no win/level-complete/game-over overlay in any slot. Each slot only calls the per-app start-gameplay path at a unique level; theme is varied per board so palettes show while the game is played. Gated by scripts/check_screenshots_gameplay_only.py.",
  "_readiness_expr": "typeof state !== 'undefined' && typeof initGame === 'function' && typeof showScreen === 'function' && typeof window.setActiveTheme === 'function' && typeof window.applyUITheme === 'function'",
  "_setup_taps": [
    [
      "js",
      "/*screenshot-clean-prefix*/try{localStorage.setItem('tutorialDone','1');localStorage.setItem('coachmarkDone','1');if(window.Android&&Android.hideBannerAd)Android.hideBannerAd();}catch(e){}(function(){try{localStorage.setItem('xstarter_seen','1');localStorage.setItem('ls_v2',JSON.stringify({n:7,d:new Date().toISOString().slice(0,10),ach:[]}));}catch(e){}try{var s={coins:247,lives:5,currentLevel:120,completedLevels:Array.from({length:118},function(_,i){return i+1;}),levelStars:(function(){var x={};for(var i=1;i<=118;i++)x[i]=(i%4===0?3:(i%3===0?2:1));return x;})(),removeAds:true,activeTheme:'default',hintPack:0,seasonPassUntil:0,weeklyPassUntil:0,lastSeasonGrantDate:'',lastWeeklyGrantDate:'',unlimitedLivesUntil:0,lastLifeTime:Date.now(),dailyChallengeDate:'',dailyChallengeStreak:7,soundEnabled:true,musicEnabled:false,hapticsEnabled:true,lastLevelProgress:null};Object.assign(state,s);if(typeof saveState==='function')saveState();if(typeof updateHUD==='function')updateHUD();if(typeof updateMenuScreen==='function')updateMenuScreen();}catch(e){}return 'seeded';})()",
      400
    ]
  ],
  "01_gameplay_open": [
    [
      "js",
      "applyUITheme('light');window.setActiveTheme('neon');showScreen('gameScreen');initGame(9,false);'ok';",
      2600
    ]
  ],
  "02_gameplay_early": [
    [
      "js",
      "applyUITheme('midnight');window.setActiveTheme('galaxy');showScreen('gameScreen');initGame(118,false);'ok';",
      2600
    ]
  ],
  "03_gameplay_build": [
    [
      "js",
      "applyUITheme('light');window.setActiveTheme('ocean');showScreen('gameScreen');initGame(33,false);'ok';",
      2600
    ]
  ],
  "04_gameplay_mid": [
    [
      "js",
      "applyUITheme('light');window.setActiveTheme('pastel');showScreen('gameScreen');initGame(57,false);'ok';",
      2600
    ]
  ],
  "05_gameplay_busy": [
    [
      "js",
      "applyUITheme('midnight');window.setActiveTheme('fire');showScreen('gameScreen');initGame(81,false);'ok';",
      2600
    ]
  ],
  "06_gameplay_deep": [
    [
      "js",
      "applyUITheme('light');window.setActiveTheme('default');showScreen('gameScreen');initGame(104,false);'ok';",
      2600
    ]
  ],
  "07_gameplay_late": [
    [
      "js",
      "applyUITheme('midnight');window.setActiveTheme('neon');showScreen('gameScreen');initGame(112,false);'ok';",
      2600
    ]
  ],
  "tablet_7_01_gameplay_open": [
    [
      "js",
      "applyUITheme('light');window.setActiveTheme('fire');showScreen('gameScreen');initGame(45,false);'ok';",
      2600
    ]
  ],
  "tablet_7_02_gameplay_early": [
    [
      "js",
      "applyUITheme('light');window.setActiveTheme('galaxy');showScreen('gameScreen');initGame(95,false);'ok';",
      2600
    ]
  ],
  "tablet_10_01_gameplay_open": [
    [
      "js",
      "applyUITheme('light');window.setActiveTheme('pastel');showScreen('gameScreen');initGame(68,false);'ok';",
      2600
    ]
  ],
  "tablet_10_02_gameplay_early": [
    [
      "js",
      "applyUITheme('midnight');window.setActiveTheme('ocean');showScreen('gameScreen');initGame(88,false);'ok';",
      2600
    ]
  ]
}
